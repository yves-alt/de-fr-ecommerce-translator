# Streamlit Cloud Secrets — copy this structure into your app's Secrets panel
# (App settings → Secrets in share.streamlit.io)
#
# DO NOT commit secrets.toml — it is already listed in .gitignore.

OPENAI_API_KEY = "sk-proj-your-openai-api-key-here"
